local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local gui = Instance.new("ScreenGui", game.CoreGui)

-- LOGO (EMOJI)
local logo = Instance.new("TextButton", gui)
logo.Size = UDim2.new(0,60,0,60)
logo.Position = UDim2.new(0,20,0,200)

logo.Text = "🐿️"
logo.TextScaled = false
logo.TextSize = 30 -- nhỏ ~2/3 ban đầu
logo.Font = Enum.Font.GothamBold

-- NỀN TRẮNG MỜ BAN ĐẦU
logo.BackgroundColor3 = Color3.fromRGB(255,255,255)
logo.BackgroundTransparency = 0.4

logo.AutoButtonColor = false

-- BO TRÒN
local corner = Instance.new("UICorner", logo)
corner.CornerRadius = UDim.new(1,0)

--------------------------------------------------
-- ZOOM (CHỈ EMOJI + ĐỔI NỀN)
--------------------------------------------------
local zoomed = false

logo.MouseButton1Click:Connect(function()
	zoomed = not zoomed
	
	if zoomed then
		-- emoji to lên
		logo.TextSize = 50
		
		-- nền trắng đục
		logo.BackgroundTransparency = 0
		
	else
		-- emoji nhỏ lại
		logo.TextSize = 30
		
		-- nền trắng mờ
		logo.BackgroundTransparency = 0.4
	end
end)

--------------------------------------------------
-- DRAG MƯỢT (FIX KHÔNG KÉO ĐƯỢC)
--------------------------------------------------
local UIS = game:GetService("UserInputService")

local dragging = false
local dragStart = nil
local startPos = nil

logo.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 
	or input.UserInputType == Enum.UserInputType.Touch then
		
		dragging = true
		dragStart = input.Position
		startPos = logo.Position
	end
end)

logo.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 
	or input.UserInputType == Enum.UserInputType.Touch then
		
		dragging = false
	end
end)

UIS.InputChanged:Connect(function(input)
	if dragging and (
		input.UserInputType == Enum.UserInputType.MouseMovement 
		or input.UserInputType == Enum.UserInputType.Touch) then
		
		local delta = input.Position - dragStart
		
		logo.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)

-- MAIN FRAME (TO + VIỀN VÀNG)
local frame = Instance.new("Frame", gui)
frame.Size = UDim2.new(0,320,0,300)
frame.Position = UDim2.new(0.7,0,0.2,0)
frame.BackgroundColor3 = Color3.fromRGB(20,20,20)
frame.Visible = false
frame.Active = true
frame.Draggable = true
frame.BorderColor3 = Color3.fromRGB(255,255,0)
frame.BorderSizePixel = 2

Instance.new("UICorner", frame).CornerRadius = UDim.new(0,12)

logo.MouseButton1Click:Connect(function()
	frame.Visible = False
end)

frame:ClearAllChildren()

-- TEXT GÓC TRÁI (RAINBOW XỊN)
local topText = Instance.new("TextLabel", frame)
topText.Size = UDim2.new(1,-40,0,20)
topText.Position = UDim2.new(0,5,0,0)
topText.BackgroundTransparency = 1
topText.Text = "NgHungHub Sát Thủ Thầm Lặng - By real_NgHung"
topText.TextScaled = true
topText.TextXAlignment = Enum.TextXAlignment.Left

task.spawn(function()
	while true do
		for i=0,1,0.01 do
			topText.TextColor3 = Color3.fromHSV(i,1,1)
			task.wait(0.03)
		end
	end
end)

-- NÚT X MAIN
local mainClose = Instance.new("TextButton", frame)
mainClose.Size = UDim2.new(0,25,0,25)
mainClose.Position = UDim2.new(1,-30,0,5)
mainClose.Text = "X"
mainClose.BackgroundColor3 = Color3.fromRGB(255,0,0)
Instance.new("UICorner", mainClose).CornerRadius = UDim.new(1,0)

mainClose.MouseButton1Click:Connect(function()
	frame.Visible = false
end)

-- TITLE
local title = Instance.new("TextLabel", frame)
title.Size = UDim2.new(1,0,0,30)
title.Position = UDim2.new(0,0,0,20)
title.BackgroundTransparency = 1
title.Text = "NgHung Hub"
title.TextScaled = true

-- BIẾN
local notifyEnabled = true
local auraEnabled = false

-- NOTIFY (VIỀN VÀNG)
local notifyFrame = Instance.new("Frame", gui)
notifyFrame.Size = UDim2.new(0,260,0,60)
notifyFrame.Position = UDim2.new(1,-270,1,-80)
notifyFrame.BackgroundColor3 = Color3.fromRGB(25,25,25)
notifyFrame.Visible = false
notifyFrame.BorderColor3 = Color3.fromRGB(255,255,0)
notifyFrame.BorderSizePixel = 2

Instance.new("UICorner", notifyFrame).CornerRadius = UDim.new(0,10)

local avatar = Instance.new("ImageLabel", notifyFrame)
avatar.Size = UDim2.new(0.4,0,1,0)
avatar.BackgroundTransparency = 1
avatar.Image = "rbxthumb://type=AvatarHeadShot&id="..LocalPlayer.UserId.."&w=150&h=150"

local notifyText = Instance.new("TextLabel", notifyFrame)
notifyText.Size = UDim2.new(0.55,0,1,0)
notifyText.Position = UDim2.new(0.4,0,0,0)
notifyText.BackgroundTransparency = 1
notifyText.TextColor3 = Color3.fromRGB(255,255,255)
notifyText.TextScaled = true
notifyText.TextWrapped = true

local closeBtn = Instance.new("TextButton", notifyFrame)
closeBtn.Size = UDim2.new(0,20,0,20)
closeBtn.Position = UDim2.new(1,-25,0,5)
closeBtn.Text = "X"

closeBtn.MouseButton1Click:Connect(function()
	notifyFrame.Visible = false
end)

function notify(msg)
	if not notifyEnabled then return end
	notifyFrame.Visible = true
	notifyText.Text = msg
	
	task.delay(5,function()
		if notifyFrame.Visible then
			notifyFrame.Visible = false
		end
	end)
end

task.spawn(function()
	task.wait(1)
	notify("👋 Xin chào "..LocalPlayer.Name.."\nCảm ơn đã sử dụng script ❤️\nTikTok: @nghbt255")
end)

local function createToggle(name, posY, callback)
	local holder = Instance.new("Frame", frame)
	holder.Size = UDim2.new(0.8,0,0,40)
	holder.Position = UDim2.new(0.1,0,posY,0)
	holder.BackgroundTransparency = 1

	local label = Instance.new("TextLabel", holder)
	label.Size = UDim2.new(0.6,0,1,0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextScaled = true

	local toggle = Instance.new("TextButton", holder)
	toggle.Size = UDim2.new(0,50,0,20)
	toggle.Position = UDim2.new(1,-60,0.5,-10)
	toggle.BackgroundColor3 = Color3.fromRGB(100,100,100)
	toggle.Text = ""

	local circle = Instance.new("Frame", toggle)
	circle.Size = UDim2.new(0,20,0,20)
	circle.BackgroundColor3 = Color3.fromRGB(255,255,255)
	Instance.new("UICorner", circle)

	local state = false

	toggle.MouseButton1Click:Connect(function()
		state = not state
		
		if state then
			toggle.BackgroundColor3 = Color3.fromRGB(0,255,0)
			circle.Position = UDim2.new(1,-20,0,0)
		else
			toggle.BackgroundColor3 = Color3.fromRGB(100,100,100)
			circle.Position = UDim2.new(0,0,0,0)
		end
		
		callback(state)
	end)
end

createToggle("Thông báo",0.3,function(v)
	notifyEnabled = v
end)

createToggle("Kill Aura",0.5,function(v)
	auraEnabled = v
end)

local esp = Instance.new("TextButton", frame)
esp.Size = UDim2.new(0.8,0,0,40)
esp.Position = UDim2.new(0.1,0,0.7,0)
esp.Text = "ESP PLAYER"

esp.MouseButton1Click:Connect(function()
	for _,p in pairs(Players:GetPlayers()) do
		if p ~= LocalPlayer and p.Character then
			if not p.Character:FindFirstChild("ESP_HL") then
				local hl = Instance.new("Highlight")
				hl.Name = "ESP_HL"
				hl.FillColor = Color3.fromRGB(255,0,0)
				hl.OutlineColor = Color3.fromRGB(0,255,0)
				hl.FillTransparency = 0.2
				hl.OutlineTransparency = 0
				hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
				hl.Parent = p.Character
			end
		end
	end
	notify("ESP bật 🔥")
end)

task.spawn(function()
	while true do
		task.wait(0.15)
		if auraEnabled then
			local char = LocalPlayer.Character
			if char and char:FindFirstChild("HumanoidRootPart") then
				for _,p in pairs(Players:GetPlayers()) do
					if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
						
						p.Character.HumanoidRootPart.Size = Vector3.new(30,30,30)
						p.Character.HumanoidRootPart.Transparency = 0.5
						
						local dist = (p.Character.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude
						if dist < 100 then
							p.Character:FindFirstChildOfClass("Humanoid").Health = 0
						end
					end
				end
			end
		end
	end
end)

-- ===== ADD (KHÔNG XOÁ CODE CŨ) =====

local RunService = game:GetService("RunService")

local function applyESP(player)
	if player ~= LocalPlayer then
		player.CharacterAdded:Connect(function(char)
			task.wait(1)
			if not char:FindFirstChild("ESP_HL") then
				local hl = Instance.new("Highlight")
				hl.Name = "ESP_HL"
				hl.FillColor = Color3.fromRGB(255,0,0)
				hl.OutlineColor = Color3.fromRGB(0,255,255)
				hl.FillTransparency = 0.3
				hl.OutlineTransparency = 0
				hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
				hl.Parent = char
			end
		end)
	end
end

for _,p in pairs(Players:GetPlayers()) do
	applyESP(p)
end

Players.PlayerAdded:Connect(function(p)
	applyESP(p)
end)

local auraRadius = 9999

task.spawn(function()
	while true do
		task.wait(0.05)
		if auraEnabled then
			local char = LocalPlayer.Character
			if char and char:FindFirstChild("HumanoidRootPart") then
				
				for _,p in pairs(Players:GetPlayers()) do
					if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
						
						local targetHRP = p.Character.HumanoidRootPart
						local humanoid = p.Character:FindFirstChildOfClass("Humanoid")
						
						if humanoid and humanoid.Health > 0 then
							targetHRP.Size = Vector3.new(250, 250, 250)
							targetHRP.Transparency = 1
							
							local dist = (targetHRP.Position - char.HumanoidRootPart.Position).Magnitude
							
							if dist <= auraRadius then
								humanoid:TakeDamage(500)
							end
						end
					end
				end
			end
		end
	end
end)

game:GetService("UserInputService").InputBegan:Connect(function(input, gp)
	if gp then return end
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		if auraEnabled then
			for _,p in pairs(Players:GetPlayers()) do
				if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Humanoid") then
					p.Character.Humanoid.Health = 0
				end
			end
			notify("Aura Burst Map")
		end
	end
end)

task.spawn(function()
	while true do
		frame.BackgroundColor3 = Color3.fromHSV(tick()%5/5, 0.6, 0.2)
		task.wait(0.1)
	end
end)
-- ===== UI MAIN CENTER + RAINBOW BORDER UPDATE =====

local TweenService = game:GetService("TweenService")

-- CENTER MAIN
frame.Position = UDim2.new(0.5, -160, 0.5, -150)

-- NỀN ĐEN + CHỮ TRẮNG
frame.BackgroundColor3 = Color3.fromRGB(0,0,0)

-- RAINBOW BORDER
task.spawn(function()
	while true do
		for i=0,1,0.01 do
			frame.BorderColor3 = Color3.fromHSV(i,1,1)
			task.wait(0.02)
		end
	end
end)

-- TOP BAR (2/10)
local topBar = Instance.new("Frame", frame)
topBar.Size = UDim2.new(1,0,0.2,0)
topBar.BackgroundColor3 = Color3.fromRGB(0,0,0)

-- TITLE
local topTitle = Instance.new("TextLabel", topBar)
topTitle.Size = UDim2.new(0.7,0,1,0)
topTitle.Position = UDim2.new(0,5,0,0)
topTitle.BackgroundTransparency = 1
topTitle.Text = "NghungHub by hynixyeuem [Premium]"
topTitle.TextColor3 = Color3.fromRGB(255,255,255)
topTitle.TextScaled = true
topTitle.TextXAlignment = Enum.TextXAlignment.Left

-- BUTTONS (- ☐ X)
local function createTopBtn(txt, posX)
	local b = Instance.new("TextButton", topBar)
	b.Size = UDim2.new(0,30,0,30)
	b.Position = UDim2.new(1,posX,0,5)
	b.Text = txt
	b.TextColor3 = Color3.fromRGB(255,255,255)
	b.BackgroundColor3 = Color3.fromRGB(0,0,0)
	return b
end

local btnMin = createTopBtn("-", -90)
local btnZoom = createTopBtn("☐", -60)
local btnClose = createTopBtn("X", -30)

-- MINIMIZE
btnMin.MouseButton1Click:Connect(function()
	frame.Visible = false
end)

-- ZOOM
local zoomMain = false
btnZoom.MouseButton1Click:Connect(function()
	zoomMain = not zoomMain
	
	if zoomMain then
		frame.Size = UDim2.new(0,500,0,450)
	else
		frame.Size = UDim2.new(0,320,0,300)
	end
end)

-- CONFIRM CLOSE UI
local confirm = Instance.new("Frame", gui)
confirm.Size = UDim2.new(0,300,0,150)
confirm.Position = UDim2.new(0.5,-150,0.5,-75)
confirm.BackgroundColor3 = Color3.fromRGB(0,0,0)
confirm.Visible = false

Instance.new("UICorner", confirm)

local cText = Instance.new("TextLabel", confirm)
cText.Size = UDim2.new(1,0,0.6,0)
cText.BackgroundTransparency = 1
cText.TextColor3 = Color3.fromRGB(255,255,255)
cText.TextScaled = true
cText.Text = "Script hiện đang hoạt động\nBạn có muốn dừng?"

local line = Instance.new("Frame", confirm)
line.Size = UDim2.new(1,0,0,2)
line.Position = UDim2.new(0,0,0.6,0)
line.BackgroundColor3 = Color3.fromRGB(255,255,255)

local yes = Instance.new("TextButton", confirm)
yes.Size = UDim2.new(0.5,0,0.4,0)
yes.Position = UDim2.new(0,0,0.6,0)
yes.Text = "Yes"
yes.BackgroundColor3 = Color3.fromRGB(0,0,0)
yes.TextColor3 = Color3.fromRGB(255,255,255)

local no = Instance.new("TextButton", confirm)
no.Size = UDim2.new(0.5,0,0.4,0)
no.Position = UDim2.new(0.5,0,0.6,0)
no.Text = "No"
no.BackgroundColor3 = Color3.fromRGB(0,0,0)
no.TextColor3 = Color3.fromRGB(255,255,255)

btnClose.MouseButton1Click:Connect(function()
	confirm.Visible = true
end)

yes.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

no.MouseButton1Click:Connect(function()
	confirm.Visible = false
end)

-- ===== SIDEBAR =====
local side = Instance.new("Frame", frame)
side.Size = UDim2.new(0.25,0,0.8,0)
side.Position = UDim2.new(0,0,0.2,0)
side.BackgroundColor3 = Color3.fromRGB(0,0,0)

local lineV = Instance.new("Frame", frame)
lineV.Size = UDim2.new(0,2,0.8,0)
lineV.Position = UDim2.new(0.25,0,0.2,0)
lineV.BackgroundColor3 = Color3.fromRGB(255,255,255)

local infoBtn = Instance.new("TextButton", side)
infoBtn.Size = UDim2.new(1,0,0.5,0)
infoBtn.Text = "Info"
infoBtn.TextColor3 = Color3.fromRGB(255,255,255)
infoBtn.BackgroundColor3 = Color3.fromRGB(0,0,0)

local farmBtn = Instance.new("TextButton", side)
farmBtn.Size = UDim2.new(1,0,0.5,0)
farmBtn.Position = UDim2.new(0,0,0.5,0)
farmBtn.Text = "Farming"
farmBtn.TextColor3 = Color3.fromRGB(255,255,255)
farmBtn.BackgroundColor3 = Color3.fromRGB(0,0,0)

local lineH = Instance.new("Frame", side)
lineH.Size = UDim2.new(1,0,0,2)
lineH.Position = UDim2.new(0,0,0.5,0)
lineH.BackgroundColor3 = Color3.fromRGB(255,255,255)

-- CONTENT
local content = Instance.new("Frame", frame)
content.Size = UDim2.new(0.75,0,0.8,0)
content.Position = UDim2.new(0.25,0,0.2,0)
content.BackgroundTransparency = 1

-- INFO PANEL
local infoPanel = Instance.new("Frame", content)
infoPanel.Size = UDim2.new(1,0,1,0)
infoPanel.BackgroundTransparency = 1

local avatar2 = Instance.new("ImageLabel", infoPanel)
avatar2.Size = UDim2.new(0,80,0,80)
avatar2.Position = UDim2.new(0,10,0,10)
avatar2.Image = avatar.Image
avatar2.BackgroundTransparency = 1

local infoText = Instance.new("TextLabel", infoPanel)
infoText.Size = UDim2.new(1,-100,1,0)
infoText.Position = UDim2.new(0,100,0,0)
infoText.BackgroundTransparency = 1
infoText.TextColor3 = Color3.fromRGB(255,255,255)
infoText.TextScaled = true
infoText.TextXAlignment = Enum.TextXAlignment.Left

task.spawn(function()
	while true do
		local t = os.date("%H:%M:%S")
		infoText.Text = "UID: "..LocalPlayer.UserId..
		"\nTime: "..t.." (Asia/HoChiMinh)"..
		"\nTikTok: @nghbt255"..
		"\nZalo: 0394310186"
		task.wait(1)
	end
end)

-- FARM PANEL
local farmPanel = Instance.new("Frame", content)
farmPanel.Size = UDim2.new(1,0,1,0)
farmPanel.Visible = false
farmPanel.BackgroundTransparency = 1

local title1 = Instance.new("TextLabel", farmPanel)
title1.Size = UDim2.new(1,0,0,30)
title1.Text = "Kill Player"
title1.TextColor3 = Color3.fromRGB(255,255,255)
title1.BackgroundTransparency = 1
title1.TextScaled = true

local btn1 = Instance.new("TextButton", farmPanel)
btn1.Size = UDim2.new(1,0,0,30)
btn1.Position = UDim2.new(0,0,0.15,0)
btn1.Text = "Định vị player"
btn1.BackgroundColor3 = Color3.fromRGB(30,30,30)
btn1.TextColor3 = Color3.fromRGB(255,255,255)

local btn2 = Instance.new("TextButton", farmPanel)
btn2.Size = UDim2.new(1,0,0,30)
btn2.Position = UDim2.new(0,0,0.3,0)
btn2.Text = "Tăng hitbox"
btn2.BackgroundColor3 = Color3.fromRGB(30,30,30)
btn2.TextColor3 = Color3.fromRGB(255,255,255)

local title2 = Instance.new("TextLabel", farmPanel)
title2.Size = UDim2.new(1,0,0,30)
title2.Position = UDim2.new(0,0,0.5,0)
title2.Text = "Thông báo script"
title2.TextColor3 = Color3.fromRGB(255,255,255)
title2.BackgroundTransparency = 1
title2.TextScaled = true

-- SWITCH TAB
infoBtn.MouseButton1Click:Connect(function()
	infoPanel.Visible = true
	farmPanel.Visible = false
end)

farmBtn.MouseButton1Click:Connect(function()
	infoPanel.Visible = false
	farmPanel.Visible = true
end)